var rem = require('gitRemoteCommand');
var rM = require('refs');

/*rem.getRemotesNames('.git', function(err, names) {
	if (err) {
		console.log(err);
		return;
	}
	console.log(names);
	return;
});

rem.getRemoteBranchDetails('.git', 'origin', 'refs_remote_manager', function(err, det) {
	if (err) {
		console.log(err);
		return;
	}
	console.log(det);
	return;
});
*/
rem.addNewRemote('.git', 'origin', 'p', null, 'i', null, function(err) {
	if (err) {
		console.log(err);
		return;
	}
//	rem.removeRemote('.git', 'origin', function(err) {
//		if (err) {
//			console.log(err);
//			return;
//		}
//	});
});
