var rem = require('gitRemoteCommand');
var rM = require('refs');

/*rem.getRemotesNames('.git', function(err, names) {
	if (err) {
		console.log(err);
		return;
	}
	console.log(names);
	return;
});

rem.getRemoteBranchDetails('.git', 'origin', 'refs_remote_manager', function(err, det) {
	if (err) {
		console.log(err);
		return;
	}
	console.log(det);
	return;
});
*/
rem.getRemoteBranches('.git', 'origin', function(err, d) {
	if (err) {
		console.log(err);
		return;
	}
	console.log(d);
})